Pull the latest version of claude-statusline from GitHub.

Run this command via Bash:

bash <(curl -fsSL https://raw.githubusercontent.com/Flagrare/claude-statusline/main/update.sh)

Report the output to the user.
