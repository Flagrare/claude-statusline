Pull the latest version of claude-statusline from GitHub.

Run this command via Bash, using the install directory (derived from ~/.claude/settings.json statusLine.command path):

cd INSTALL_DIR && git pull origin main

Report the output to the user.
